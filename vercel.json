{
  "framework": "nextjs",
  "buildCommand": "prisma generate && npm run build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "crons": {
    "/api/cron/monthly": {
      "schedule": "0 0 1 * *"
    }
  }
}
